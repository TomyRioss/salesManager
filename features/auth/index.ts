export { Auth } from './view/Auth'
export { login, register, logout, getSession } from './controller/auth.controller'
export type { LoginData, RegisterData, SessionUser, AuthResult } from './model/types'
